<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
		<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" integrity="sha384-JcKb8q3iqJ61gNV9KGb8thSsNjpSL0n8PARn9HuZOnIxN0hoP+VmmDGMN5t9UJ0Z" crossorigin="anonymous"> 
        <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
        <title>{% block title %}Joyeux anniversaire !{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        {# <nav class="navbar navbar-expand-lg navbar-light bg-light mb-5">
            <a class="navbar-brand mb-0 h1" href="{{ path('homepage') }}">
                &#128525; Accueil et météo
            </a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('relative_list') }}">Liste des proches</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('home_list') }}">Liste des foyers</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('event_list') }}">Anniversaires</a>
                    </li>
                </ul>
            </div>
        </nav> #}

        <nav class="navbar navbar-expand-lg navbar-light bg-light mb-5">
            {% if app.user %}
            <div class="mx-auto order-0">
                <a class="navbar-brand mb-0 h1" href="{{ path('homepage') }}">
                    &#128525; Accueil et météo
                </a>
                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target=".dual-collapse2">
                    <span class="navbar-toggler-icon"></span>
                </button>
            </div>
            <div class="navbar-collapse collapse w-100 order-1 order-md-0 dual-collapse2">
                <ul class="navbar-nav mr-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('relative_list') }}">Liste des proches</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('home_list') }}">Liste des foyers</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('event_list') }}">Anniversaires</a>
                    </li>
                </ul>
            </div>
            {% else %}
            <div class="mx-auto order-0">
                <a class="navbar-brand mb-0 h1" href="{{ path('homepage') }}">
                    &#128525; Bienvenue sur la météo des proches !
                </a>
                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target=".dual-collapse2">
                    <span class="navbar-toggler-icon"></span>
                </button>
            </div>
            {% endif %}
            <div class="navbar-collapse collapse w-100 order-3 dual-collapse2">
                {% if app.user %}
                    <ul class="navbar-nav ml-auto">
                        <li class="nav-item">
                            Connecté-e en tant que {{ app.user.username|capitalize }}, <a href="{{ path('app_logout') }}">Se déconnecter</a>
                        </li>
                    </ul>
                {% else %}
                    <ul class="navbar-nav ml-auto">
                        <li class="nav-item">
                            <a href="{{ path('app_login') }}">Se connecter</a>
                        </li>
                    </ul>
                {% endif %}
            </div>
        </nav>

        <div class="container">
            {% for label, messages in app.flashes %}
                {% for message in messages %}
                    <div class="alert alert-{{ label }}">
                        {{ message }}
                    </div>
                {% endfor %}
            {% endfor %}

            {% block body %}{% endblock %}
        </div>

		
		<script src="https://code.jquery.com/jquery-3.5.1.slim.min.js" integrity="sha384-DfXdz2htPH0lsSSs5nCTpuj/zy4C+OGpamoFVy38MVBnE+IbbVYUew+OrCXaRkfj" crossorigin="anonymous"></script>
		<script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.1/dist/umd/popper.min.js" integrity="sha384-9/reFTGAW83EW2RDu2S0VKaIzap3H66lZH81PoYlFhbGU+6BZp6G7niu735Sk7lN" crossorigin="anonymous"></script>
		<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js" integrity="sha384-B4gt1jrGC7Jh4AgTPSdUtOBvfO8shuf57BaghqFfPlYxofvL8/KUEfYiJOMMV+rV" crossorigin="anonymous"></script>

        {% block javascripts %}{% endblock %}
    </body>
</html>
